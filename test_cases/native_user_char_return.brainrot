🚽 Test case: round-23 review finding #3, the char sibling of
🚽 native_user_string_return.brainrot -- `yap f() { bussin 'A'; }` hit
🚽 the identical "Unsupported return type" producer gap, without the
🚽 string-ownership complication (a char return owns no separate heap
🚽 allocation, so this exercises the missing VAR_CHAR case in isolation).
yap letter() {
    bussin 'A';
}

skibidi main {
    yap c = letter();
    yappin("%c\n", c);
    bussin 0;
}

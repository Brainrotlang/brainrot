🚽 Test case: takes_char() declares a plain STDROT_CHAR parameter.
🚽 buf[0] (NODE_ARRAY_ACCESS on a char array) is source-level VAR_CHAR,
🚽 but ast_expr_to_stdrot_value() marshals array access through plain
🚽 int regardless of element type (stdrot_char_narrows_to_int(),
🚽 stdrot.h -- matching C's own integer-promotion rules). Before
🚽 infer_expression_type() had a NODE_ARRAY_ACCESS case at all, this
🚽 call's argument type was unconditionally NONE, silently skipping
🚽 argument checking entirely; even after adding that case, the char-
🚽 narrows-to-int rule has to apply here too, or this call would be
🚽 statically approved and only caught later by enforce_arg_type() at
🚽 the runtime ABI boundary -- a confusing "ABI violation" for what's
🚽 actually an ordinary, staticaly-detectable type mismatch.
skibidi main {
    yap buf[32];
    slorp(buf);
    rizz r = takes_char(buf[0]);
    yapping("%d", r);
    bussin 0;
}

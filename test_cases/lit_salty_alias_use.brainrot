🚽 Test case: a storage-class modifier on an alias use belongs to the
🚽 declaration, not the typedef alias itself.
lit rizz Count;

rizz next_count() {
    salty Count count = 0;
    count = count + 1;
    bussin count;
}

skibidi main {
    yapping("%d %d", next_count(), next_count());
    bussin 0;
}

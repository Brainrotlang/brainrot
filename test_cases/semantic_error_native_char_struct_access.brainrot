🚽 Test case: takes_char() declares a plain STDROT_CHAR parameter.
🚽 f.c (NODE_STRUCT_ACCESS on a char field) is source-level VAR_CHAR,
🚽 but ast_expr_to_stdrot_value() marshals struct access through plain
🚽 int regardless of field type (the same stdrot_char_narrows_to_int()
🚽 rule as array access). infer_expression_type()'s NODE_STRUCT_ACCESS
🚽 case previously only worked at runtime (resolve_struct_access()
🚽 depends on get_variable(), unavailable during the single static
🚽 analysis pass) -- unconditionally NONE statically, silently skipping
🚽 argument checking. Now resolved via the analyzer's own symbol table
🚽 + the struct type registry (both independent of any runtime Variable
🚽 instance), and the char-narrows-to-int rule applied on top.
gang Foo {
    yap c;
};

skibidi main {
    gang Foo f = { 'x' };
    rizz r = takes_char(f.c);
    yapping("%d", r);
    bussin 0;
}

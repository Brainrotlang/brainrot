🚽 Test case: round-18 review finding #2, exhibit A -- get_native_call_
🚽 static_type()'s identity-argument case (ast.c) must report the ABI
🚽 type a native argument actually marshals as, not the plain source-
🚽 level type get_expression_type() reports. A char *array* identifier
🚽 (`yap buf[32]`) marshals as STDROT_STRING (ast_expr_to_stdrot_value()'s
🚽 VAR_CHAR/is_array branch, stdrot.c), not the element type VAR_CHAR --
🚽 so identity(buf) statically resolves to VAR_STRING, which handle_
🚽 sizeof()'s type-dispatch switch has no case for (a native STRING
🚽 result has no fixed C-level size the way a scalar does), and correctly
🚽 rejects. Confirmed via a temporary A/B test (reverting the identity
🚽 case back to plain get_expression_type()) that the pre-fix code
🚽 silently reported 1 (sizeof(char), the array's ELEMENT type) instead
🚽 of rejecting -- a wrong answer with no error at all, not just a
🚽 differently-wrong one.
skibidi main {
    yap buf[32];
    rizz n = maxxing(identity(buf));
    bussin 0;
}
